### CoreScanner C# Snippets Compilation/Execution

Install CoreScanner/Scanner SDK.

Create a C# .Net Framework Console Application.
 
Add a Reference to the “CoreScanner Type Library” 
(Right click project references in Visual Studio and select “Add Reference”, in COM section select “CoreScanner Type Library”).

Add the code snippet, compile and execute application.
